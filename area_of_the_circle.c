#include <stdio.h>
#include <math.h>
int main()
{
    float r=3.14,p=3.14;
    // float R = (p*(r*r));
    float R2;
    R2=sqrt(r/p);
    // printf("R = %f\n",R);
    printf("R2 = %f\n",R2);
    return 0;
}